dmenu + systemctl --user
************************

A simple script that uses systemctl and dmenu to make managing your user
units easy without opening up a terminal. (Though it can do that if you
want.)
